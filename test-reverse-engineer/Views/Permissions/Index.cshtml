@model IEnumerable<test_reverse_engineer.Models.Permission>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PermissionId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PermissionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PermissionCreatedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PermissionModifiedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionCreatedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionModifiedDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PermissionId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PermissionId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PermissionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
